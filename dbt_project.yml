name: complaint_analytics
version: "1.0.0"
config-version: 2

profile: complaint_analytics

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  complaint_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
